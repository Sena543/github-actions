name: first-github-action
on: [push]
jobs:
    write-to-console:
        runs-on: ubuntu-latest
        steps:
            - name: Setup Node
            - uses: actions/checkout@v2
            - uses: actions/setup-node@v2
              with:
                  node-version: "14"
            - name: Run command
            - run: echo "Another section 😉"
            - run: echo 'Second run command for action'
